XX callbacks shuold allow for (!) to cancel
XX Should be an easy way to return to menu from all screens (!)
XX Can we abort / fix

auto-refresh on callbacks pages? 
multi dance should validate dancers, so that the same dancers are in each dance
XXAdd separate data file with time stamps
